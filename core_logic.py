def process_message_internal(user_id, user_message):
    # Dummy response, replace with real Claude logic
    return {
        "user_id": user_id,
        "input": user_message,
        "response": "Hello from WhatsApp bot! Your message was: " + user_message
    }
